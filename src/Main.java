public class Main {
    public static void main(String[] args) {
                long index = 5;
        long result = triton(index); //
        System.out.println("\nResult of index " + index + " is "
            + result);

        index = 10;
        long result = triton(index); //
        System.out.println("\nResult of index " + index + " is "
               + result);
    }
}
